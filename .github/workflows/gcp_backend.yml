name: Deploy Backend to Cloud RUN

# TODO: determine if changes to backend folder before pushing

on:
  push:
    branches: [ "main", "development", "ft/cicd" ]
    paths:
      - 'backend/**'
      - '.github/workflows/gcp_backend.yml'

env:
  SERVICE: backend
  REGION: us-central1
  BACKEND_IMAGE_NAME: foxxy


jobs:
  deploy:
    environment: ${{ (github.ref == 'refs/heads/development' && 'development') || (github.ref == 'refs/heads/main' && 'prod') }}
    permissions:
      contents: 'read'
      id-token: 'write'

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Google Auth
        id: auth
        uses: 'google-github-actions/auth@v0'
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}
      - run: gcloud auth configure-docker
      - name: Set up Environment Variables
        run: |
          set -x;
          echo ${DOCKER_NAMESPACE_DEVELOPMENT}
          if [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            export DOCKER_NAMESPACE=${{var.DOCKER_NAMESPACE_PRODUCTION}}
          elif [[ "${GITHUB_REF_NAME}" == "development" ]]; then
            export DOCKER_NAMESPACE=${{var.DOCKER_NAMESPACE_DEVELOPMENT}}
          else
            export DOCKER_NAMESPACE=${{var.DOCKER_NAMESPACE_DEVELOPMENT}}
          fi
      - name: Build and Push Docker image
        run: |

          IMAGE_TAG="0.0.${GITHUB_RUN_NUMBER}"
          export IMAGE="krccr.ccs.tencentyun.com/${DOCKER_NAMESPACE}/${BACKEND_IMAGE_NAME}:${IMAGE_TAG}"
          export IMAGE_LATEST="krccr.ccs.tencentyun.com/${DOCKER_NAMESPACE}/${BACKEND_IMAGE_NAME}:latest"
          docker build -t krccr.ccs.tencentyun.com/${DOCKER_NAMESPACE}/${BACKEND_IMAGE_NAME} -f backend/Dockerfile ./backend/
          docker push ${IMAGE}
          docker push ${IMAGE_LATEST}
      # - name: Deploy to Cloud Run
      #   id: deploy
      #   uses: google-github-actions/deploy-cloudrun@v0
      #   with:
      #     service: ${{ env.SERVICE }}
      #     region: ${{ env.REGION }}
      #     image: gcr.io/${{ vars.GCP_PROJECT_ID }}/${{ env.SERVICE }}

      # If required, use the Cloud Run url output in later steps
      # - name: Show Output
      #   run: echo ${{ steps.deploy.outputs.url }}